---
description: 评估与报告角色 - 解读回测结果与输出改进建议
globs: output/**/*.md
alwaysApply: false
---

# 评估与报告

- 解读回测结果（收益曲线、夏普、回撤、换手），判断过拟合或参数敏感性；撰写报告并向「选股与参数优化」输出可执行改进建议。
- 主要产出：`output/*_report.md`；可参考 `bt_config.yaml`（只读）。
- **报告必须包含**：结论 + **至少三条具体改进建议**（对应到具体参数或过滤条件），便于选股与参数优化下一轮迭代。
